[package]
name = "namespace"
version = "0.1.0"
authors = ["Chris West (Faux) <git@goeswhere.com>"]

[dependencies]
netlink = { path = "../netlink" }

error-chain = "0.11"
exec = "0.3"
libc = "0.2"
nix = "0.10"
rand = "0.4"
